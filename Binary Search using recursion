class BinarySearch {
    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 4,5};
        int target = 2;
        int start=0;
        int end=arr.length;
        System.out.println("Target number index: " + binsearch(arr, target,start,end));
    }
    public static int binsearch(int[] arr, int target, int start, int end) {
        int mid=(start+end)/2;
        if(start>end)
        {
            return 0; 
        }
        if(target==arr[mid])
        {
            return mid;
        }
        else if(arr[mid]>target)
        {
            end=mid-1;
        }
        else if(arr[mid]<target)
        {
            start=mid+1;
        }
        return binsearch(arr,target,start,end);
    }
}
